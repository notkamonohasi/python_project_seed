[project]
name = "python-project-seed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.2",
    "mypy>=1.12.1",
    "pydantic>=2.9.2",
    "pytest>=8.3.3",
    "ruff>=0.7.0",
]
